import React from 'react';
import styled from 'styled-components';

const Footer = () => {
  return <Wrapper>Footer</Wrapper>;
};

export default Footer;

const Wrapper = styled.div`
  background-color: #bde8ff; //임시
  width: 100%;
  height: 200px; //들어갈 내용에 따라 변동 가능
`;
